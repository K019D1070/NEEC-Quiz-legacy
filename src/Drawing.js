import top from "../resources/html/top.html";
export default class Drawing{
  constructor(page){
    if(page != undefined){
      this.draw(page);
    }
  }
  draw(p){
    //document.getElementsByTagName("main")[0].innerHTML = ;
  }
}